<article>
  <%= render "admin/shared/visit_button", site: @site %>
  <section>
    <header>
      <h2><span>É</span>dition de la page</h2>
    </header>
    <div class="container">
      <div class="col">
        <%= link_to admin_site_pages_path, class: "inline-flex items-center gap-2 mb-4 rounded bg-gray-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500" do %>
          <i class="fa-solid fa-arrow-left"></i>
          Retour
        <% end %>
        <%= render "shared/error_messages", resource: @page %>
        <%= render "form", site: @site, page: @page %>
      </div>
    </div>
  </section>
</article>
