<%= link_to (defined?(@page) && @page.present? ? "/#{@site.slug}/#{@page.slug}" : "/#{@site.slug}"),
            target: "_blank",
            class: "visit inline-block",
            role: "button",
            title: "Voir le site" do %>
  <i class="fas fa-eye"></i>
<% end %>
